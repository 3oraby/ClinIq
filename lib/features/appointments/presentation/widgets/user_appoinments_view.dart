import 'package:flutter/material.dart';

class UserAppointmentsView extends StatelessWidget {
  const UserAppointmentsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Appointments');
  }
}
